import { useState } from "react";
import { Link, useLocation } from "wouter";
import { ArrowRight, ArrowUpRight, CalendarClock, Check, FilePlus2, LayoutDashboard, LogOut, Moon, Search, Shield, Sun, Trash2 } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Logo } from "../public/ReaderPages";
import { useTheme } from "../contexts/ThemeContext";
import NotFound from "../pages/NotFound";

function ThemeToggle(){const {theme,toggleTheme}=useTheme();return <button className="admin-icon-button" onClick={toggleTheme} aria-label="Toggle theme">{theme==="light"?<Moon size={17}/>:<Sun size={17}/>}</button>}
export function AdminLogin(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const login=trpc.admin.login.useMutation({onSuccess:()=>location.assign("/admin"),onError:()=>location.assign("/404")});return <main className="admin-auth"><div className="admin-auth-art"><Logo/><div><span className="eyebrow">Private newsroom</span><h1>Make the<br/><em>signal</em> matter.</h1><p>A focused editorial desk for shaping the next considered brief.</p></div><span className="auth-quote">“Clarity is an editorial choice.”</span></div><section className="admin-auth-form"><div className="admin-auth-top"><Logo compact/><ThemeToggle/></div><div className="admin-form-card"><span className="eyebrow">Newsroom access</span><h2>Welcome back</h2><p>Sign in to shape the next edition of Aurikrex Bytes.</p><form onSubmit={e=>{e.preventDefault();login.mutate({email,password,remember:true})}}><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="button button-full" type="submit">Continue <ArrowRight size={16}/></button></form></div></section></main>}
function AdminFrame({children,title,role}:{children:React.ReactNode;title:string;role:string}){return <main className="admin-app"><aside className="admin-rail"><Logo compact/><div className="admin-rail-title">Newsroom<span>{role}</span></div><nav><Link href="/admin"><LayoutDashboard size={16}/> Inbox</Link><Link href="/admin/new"><FilePlus2 size={16}/> New post</Link>{role==="admin"&&<><Link href="/admin/team"><Shield size={16}/> Team</Link><Link href="/admin/analytics"><CalendarClock size={16}/> Analytics</Link></>}</nav><div className="admin-rail-bottom"><Link href="/"><ArrowUpRight size={15}/> Public brief</Link><ThemeToggle/></div></aside><section className="admin-main"><header className="admin-header"><div><span className="eyebrow">Editorial desk</span><h1>{title}</h1></div><span className="admin-role-badge">{role}</span></header>{children}</section></main>}
const labels:any={draft:"Draft",pending_review:"Pending review",scheduled:"Scheduled",published:"Published"};
export function AdminDashboard(){const session=trpc.admin.session.useQuery();const posts=trpc.admin.posts.useQuery(undefined,{enabled:Boolean(session.data)});const utils=trpc.useUtils();const [filter,setFilter]=useState("all");const [query,setQuery]=useState("");const remove=trpc.admin.deletePost.useMutation({onSuccess:()=>void utils.admin.posts.invalidate()});const unschedule=trpc.admin.unschedulePost.useMutation({onSuccess:()=>void utils.admin.posts.invalidate()});if(session.isLoading)return <div className="admin-loading">Loading newsroom…</div>;if(!session.data)return <NotFound/>;const shown=(posts.data||[]).filter((p:any)=>(filter==="all"||p.status===filter)&&(`${p.headline} ${p.body}`.toLowerCase().includes(query.toLowerCase())));return <AdminFrame title="Publishing inbox" role={session.data.role}><div className="admin-actions"><div className="admin-search"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search posts"/></div><Link className="button" href="/admin/new">New post <ArrowRight size={15}/></Link></div><div className="admin-tabs">{[["all","All"],["scheduled","Scheduled"],["pending_review","Pending review"],["published","Published"],["draft","Drafts"]].map(([key,label])=><button className={filter===key?"active":""} key={key} onClick={()=>setFilter(key)}>{label}<span>{key==="all"?posts.data?.length:(posts.data||[]).filter((p:any)=>p.status===key).length}</span></button>)}</div><section className="admin-table-card"><div className="admin-table-head"><span>Story</span><span>Status</span><span>Updated</span><span>Actions</span></div>{shown.length?shown.map((post:any)=><div className="admin-table-row" key={post.id}><div className="admin-story"><div className="admin-thumb">{post.imageUrl?<img src={post.imageUrl} alt=""/>:<Spark/>}</div><div><Link href={`/admin/preview/${post.id}`}><strong>{post.headline}</strong></Link><small>{post.body.slice(0,90)}{post.body.length>90?"…":""}</small></div></div><span className={`status-badge ${post.status}`}>{labels[post.status]}</span><span className="admin-date">{post.scheduledTime?new Date(post.scheduledTime).toLocaleString():new Date(post.updatedAt).toLocaleDateString()}</span><div className="row-actions"><Link className="text-button" href={`/admin/preview/${post.id}`}>Preview</Link>{post.status==="scheduled"&&session.data.role==="admin"&&<button className="text-button" onClick={()=>unschedule.mutate({id:post.id})}>Cancel</button>}{session.data.role==="admin"&&<button className="danger-button" onClick={()=>remove.mutate({id:post.id})} aria-label="Delete post"><Trash2 size={15}/></button>}</div></div>):<div className="admin-empty"><Check size={20}/><h2>No posts in this view</h2><p>Try another filter or create a new draft.</p></div>}</section></AdminFrame>}
function Spark(){return <span className="spark-mark">✦</span>}
