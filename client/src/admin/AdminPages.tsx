import { useState } from "react";
import { Link } from "wouter";
import { trpc } from "../lib/trpc";
import NotFound from "../pages/NotFound";
import CreatePostPanel from "./CreatePostPanel";

export function AdminLogin() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const login = trpc.admin.login.useMutation({ onSuccess: () => location.assign("/admin"), onError: () => location.assign("/404") }); return <main className="auth-wrap"><section className="auth-panel newsroom"><p className="eyebrow">Newsroom</p><h1>Editorial access</h1><form onSubmit={e => { e.preventDefault(); login.mutate({ email, password, remember: true }); }}><label>Email<input type="email" onChange={e => setEmail(e.target.value)} required /></label><label>Password<input type="password" onChange={e => setPassword(e.target.value)} required /></label><label className="check"><input type="checkbox" defaultChecked /> Remember this device</label><button className="button" type="submit">Continue</button></form></section></main>; }
export function AdminDashboard() { const session = trpc.admin.session.useQuery(); const posts = trpc.admin.posts.useQuery(undefined, { enabled: Boolean(session.data) }); if (!session.data) return <NotFound />; return <main className="admin-shell"><aside><p className="eyebrow">Aurikrex Bytes</p><h2>Newsroom</h2><p className="muted">{session.data.role}</p><Link href="/">View public brief</Link></aside><section className="admin-content"><p className="eyebrow">Editorial desk</p><h1>Publishing queue</h1><div className="metric-row"><div><strong>{posts.data?.length || 0}</strong><span>Total posts</span></div><div><strong>4</strong><span>Publishing states</span></div><div><strong>—</strong><span>Next scheduled</span></div></div><CreatePostPanel /><div className="table-card"><h2>Recent posts</h2>{posts.data?.map(post => <div className="table-row" key={post.id}><span>{post.headline}</span><span className="status">{post.status}</span></div>)}</div></section></main>; }
